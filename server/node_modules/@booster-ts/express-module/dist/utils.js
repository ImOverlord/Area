"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * loadMainPackageJSON
 * @description Searches for Main App Package.json
 * @param attempts
 */
function loadMainPackageJSON(attempts = 1) {
    attempts = attempts || 1;
    if (attempts > 5)
        throw new Error('Can\'t resolve main package.json file');
    var mainPath = attempts === 1 ? './' : Array(attempts).join("../");
    try {
        return {
            package: require.main.require(mainPath + 'package.json'),
            path: mainPath
        };
    }
    catch (e) {
        return this.loadMainPackageJSON(attempts + 1);
    }
}
exports.loadMainPackageJSON = loadMainPackageJSON;
